# Database (PostgreSQL) — в Docker-сети хост = имя сервиса db
DATABASE_URL=postgres://postgres:postgres@db:5432/intisp

# Django
SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,web
